# Base URL for backend_api (FastAPI).
# Example: http://localhost:3001
# In Kavia environment, set to the backend container URL.
NEXT_PUBLIC_BACKEND_API_BASE_URL=

# Optional: App display name (purely cosmetic).
NEXT_PUBLIC_APP_NAME="Cognitive Learning Hub"
